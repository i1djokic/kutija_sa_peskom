apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mariadb.fullname" . }}-db
  labels:
    {{- include "mariadb.labels" . | nindent 4 }}
type: Opaque
data:
  root-password: {{ .Values.mariadb.rootPassword | b64enc }}
